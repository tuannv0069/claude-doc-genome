---
class: scope
subject: .claude/rules/rule-writing-standards.md
anchor: skills/init-project/templates/rules/project-standard-maintenance.md.tpl
---
- decided: `project-standard-maintenance.md.tpl` is not adopted
- because: it binds the maintenance standard to a project's rule-bearing folders, a binding that only carries weight under the `_`-prefix folder convention this lineage does not use — the same convention behind the deferred `naming_prefix` signal
- rejected: adopt it with a new interview slot — that buys an unused slot and a render step for a rule `rule-writing-standards.md` already scopes through its own `paths:`
